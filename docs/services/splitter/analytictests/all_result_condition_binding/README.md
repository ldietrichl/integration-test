# ALL result / все связанные эксперименты + EXPLAB-2414 / привязка condition к группе

Категория соответствует блоку аналитика: **ALL result / все связанные эксперименты + EXPLAB-2414 / привязка condition к группе**.

> После EXPLAB-2690 термин «все связанные эксперименты» относится к внутреннему/отчётному результату. В публичном REST `ALL` остаются только записи реально сработавших групп (`expGroup = finalExpGroup`). Связи разных групп проверяются отдельными управляемыми split-запросами либо через Kafka/report.

